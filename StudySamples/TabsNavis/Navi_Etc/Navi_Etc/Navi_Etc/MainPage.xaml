<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Navi_Etc"
             x:Class="Navi_Etc.MainPage"
             Title="MainPage">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Key="buttonStyle" TargetType="Button">
                <Setter Property="VerticalOptions" Value="CenterAndExpand" />
                <Setter Property="TextColor" Value="White" />
                <Setter Property="FontSize" Value="20" />
                <Setter Property="BackgroundColor" Value="Black"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <StackLayout>
        <Button Text="SinglePageNavigation"
                StyleId="SinglePageNavigation"
                Style="{StaticResource buttonStyle}" 
                Clicked="NabiClicked"/>

        <Button Text="MvvmEnforcement"
                StyleId="MvvmEnforcement"
                Style="{StaticResource buttonStyle}" 
                Clicked="NabiClicked"/>

        <Button Text="StackManipulation"
                StyleId="StackManipulation"
                Style="{StaticResource buttonStyle}" 
                Clicked="NabiClicked"/>
    </StackLayout>

</ContentPage>
